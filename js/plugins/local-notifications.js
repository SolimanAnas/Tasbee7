// Capacitor local notifications plugin stub
// Full plugin available when building with Capacitor
if (typeof Capacitor !== 'undefined') {
  console.log('🔌 Capacitor local notifications plugin loaded');
}
